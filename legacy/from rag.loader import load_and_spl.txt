from rag.loader import load_and_split_pdfs
from rag.vectorstore import create_vectorstore, save_vectorstore

print("Loading PDFs...")
documents = load_and_split_pdfs("rag/data/manuals")

print(f"Total chunks created: {len(documents)}")

print("Creating vector database...")
vectorstore = create_vectorstore(documents)

print("Saving vector database...")
save_vectorstore(vectorstore, "rag/data/vectordb")

print("Done! Vector DB is ready.")